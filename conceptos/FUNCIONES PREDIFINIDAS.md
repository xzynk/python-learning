```Python
print() #Imprime valores en la consola
len() # Devuelve la longitud de un objeto
type() # Devuelve el tipo de objeto
int(),float(),str() # Convierte datos a tipos numericos o strings
sum() #Suma los elementos de un iterables (lista, tuple)
max(), min() #Devuelve el valor maximo o minimo
abs() #Devuelve el valor absoluto de un numero
round() #Redondea un numero al numero de decimales especificado
range() #Genera una secuencia de numeros
enumerate() #Genera un iterador que genera un indice y el valor correspondiente de un iterable
zip() #Combina varios iterables, creando tuplas con sus elementos correspondientes
map() #Aplica una funcion a todos los elementos de un iterable
filter() #Filtra los elementos de un iterable usando una funcion
sorted() #Devuelve una lista ordenada de un iterable
all() #Devuelve true si todas los elementos de un iterable son verdaderos
any() #Devuelve true si al menos un elemento de un iterable es verdadero
input() #Reciba una entrada de usuario
isinstance() #Verifica si un objeto es de un tipo en particular
open() #Abre un archivo de lectura o escritura
help() #Muestra la documentacion de un objeto o funcion
dir() #Muestra todos los atributos y metodos de un objeto
reversed() #Invierte el orden de una lista
```